W4.1) What is the modulus operator? Define it with example.

->The modulus operator,written in most programming language as % or mod,perform what is
known as the modulo operation.The modulus operator is a way to determine the
 reminder of a division operation.Instead of returning the result of the division,the
 modulo operation returns the whole number remainder.
 
example:
	A-B=-10
	A*B=200
	B/A=2
	B%A=0
